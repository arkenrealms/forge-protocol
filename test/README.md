# test

Node test-runner coverage for Forge protocol package.

## Coverage in this slot
- `core.router.test.js`
  - verifies successful `sync` dispatch via caller factory
  - verifies explicit missing-service error path
